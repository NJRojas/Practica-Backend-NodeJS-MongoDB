<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <header>
      <h1><%= title %></h1>
    </header>
    <h3>Bienvenido a <%= title %>, lo tenemos todo y lo que no te lo creamos 😎</h3>

    <h2>Anuncios</h2>

    <% ads.forEach(ad => { %>
      <h3><%= ad.article %></h3>
      <img src="images/ads/<%= ad.image_url %>" alt="Article image">
      <nav>
        <ul>
          <li><% if (ad.onsell) { %>
              <b>Se vende</b>
            <% } else { %>
              <b>Se busca</b>
             <% } %>
          </li>
          <li><% if (ad.onsell) { %>
            <b>Precio:</b>
            <% } else { %>
              <b> Ofrece: </b>
            <% } %>
            <%= ad.price %> <b>€</b>
          </li>
        </ul>
        <p><b>Descripción:</b> <%= ad.description %></p>
        <div>
          <% ad.tags.forEach(tag => { %>
            <span class="tag"><%= tag %></span>
          <% }) %>
        </div>
      </nav>
      <% }) %>
  </body>
</html>
